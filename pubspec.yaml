name: flutter_disposables
version: 0.0.1
description: Disposable is responsible for auto disposing resources.
homepage: https://github.com/mosamuhana
repository: https://github.com/mosamuhana/flutter_disposables
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  disposables:
    path: ../dart_disposables/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
